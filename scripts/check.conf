# scripts/check.conf — roombook verification contract.
# Stack: Next.js + TypeScript + Prisma/PostgreSQL + Vitest (decided at bootstrap, see docs/conventions.md).
#
# Activated incrementally per specs/plans/0001-plan.md: lint/typecheck/build at Step 1,
# test activated at Step 4 (requires a reachable Postgres via DATABASE_URL — see CI).

lint: npm run lint
typecheck: npm run typecheck
build: npm run build
test: npm run test
