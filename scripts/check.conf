# scripts/check.conf — roombook verification contract.
# Stack: Next.js + TypeScript + Prisma/PostgreSQL + Vitest (decided at bootstrap, see docs/conventions.md).
#
# No application scaffold exists yet (true greenfield) — these steps are commented out so
# `./scripts/check` reports a clean no-op until the first feature's plan creates package.json.
# Uncomment (and adjust script names) as soon as `npm init`/the Next.js scaffold exists:
#
# lint: npm run lint
# typecheck: npm run typecheck
# test: npm test
# build: npm run build
